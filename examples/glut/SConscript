import os

Import("env CORONA_LIBS");

# @todo  use SConscript_chdir
execfile('examples/glut/glut_libs.py')

env2 = env.Copy(CPPPATH = '#src',
                LIBPATH = '#src',
                LIBS = CORONA_LIBS + GLUT_LIBS)
env2.Program(target = 'glut', source = 'glut.cpp')
